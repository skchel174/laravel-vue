version: '3'
services:
  nginx:
    build:
      context: docker/nginx
    volumes:
      - ./:/var/www
    ports:
      - "8080:80"
    links:
      - php
  php:
    build:
      context: docker/php
    volumes:
      - ./:/var/www
